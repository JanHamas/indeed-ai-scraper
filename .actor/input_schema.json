{
  "title": "Indeed AI Job Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {

    "search_keywords": {
      "title": "🔑 Positions / Keywords for Search",
      "type": "string",
      "description": "Enter job titles or keywords to search on Indeed, one per line. The scraper builds search URLs automatically from these.\n\nExample:\nPython Developer\nBackend Engineer\nDjango Developer\n\nLeave blank if you are providing your own 'Indeed Search URLs' below.",
      "editor": "textarea",
      "prefill": ""
    },

    "search_country": {
      "title": "🌍 Country for Search",
      "type": "string",
      "description": "The country to search on Indeed. Determines which Indeed domain is used (e.g. indeed.com, uk.indeed.com).",
      "editor": "select",
      "enum": ["us", "uk", "ca", "au", "in", "de", "fr", "sg", "pk", "nl", "es", "it", "br", "mx", "nz", "jp", "ae", "sa"],
      "enumTitles": [
        "🇺🇸 United States", "🇬🇧 United Kingdom", "🇨🇦 Canada", "🇦🇺 Australia",
        "🇮🇳 India", "🇩🇪 Germany", "🇫🇷 France", "🇸🇬 Singapore",
        "🇵🇰 Pakistan", "🇳🇱 Netherlands", "🇪🇸 Spain", "🇮🇹 Italy",
        "🇧🇷 Brazil", "🇲🇽 Mexico", "🇳🇿 New Zealand", "🇯🇵 Japan",
        "🇦🇪 UAE", "🇸🇦 Saudi Arabia"
      ],
      "default": "us"
    },

    "search_location": {
      "title": "📍 Location for Search (optional)",
      "type": "string",
      "description": "City, state, or region to search within. Leave blank to search nationwide or for remote jobs.\n\nExamples:  San Francisco, CA  |  New York  |  Remote  |  London",
      "editor": "textfield",
      "prefill": ""
    },

    "start_urls": {
      "title": "🔗 Indeed Search URLs (optional — overrides keyword search)",
      "type": "array",
      "description": "Advanced option: paste your own Indeed search URLs here. These take priority over the keyword/location/country fields above.\n\nWhen to use this: if you want to use Indeed's filters (date posted, salary, job type) that aren't available through the keyword builder — just set up your search on Indeed with those filters and copy the URL.\n\nLeave blank to use the keyword search fields above.",
      "editor": "stringList",
      "prefill": []
    },

    "about_me": {
      "title": "🎯 Target Job Titles for AI Matching (optional)",
      "type": "string",
      "description": "Enter the job titles you are targeting, one per line. The AI (JobBERT) scores each scraped job title against yours — only jobs meeting your minimum score are saved.\n\nLeave blank to disable AI filtering and collect all jobs.",
      "editor": "textarea",
      "prefill": ""
    },

    "max_jobs": {
      "title": "📦 Total Jobs to Collect",
      "type": "integer",
      "description": "The scraper stops once it has saved this many jobs total (across all keywords/URLs). Recommended: 50–500.",
      "default": 50,
      "minimum": 1,
      "maximum": 500
    },

    "per_company_jobs": {
      "title": "🏢 Max Jobs per Company",
      "type": "integer",
      "description": "Limits how many jobs from the same company are saved. Keeps results diverse.",
      "default": 5,
      "minimum": 1,
      "maximum": 50
    },

    "scrape_company_details": {
      "title": "🏭 Scrape Company Details",
      "type": "boolean",
      "description": "When enabled, the scraper visits each company's Indeed profile page to collect extra data: company size, industry, and a short description.\n\nNote: this roughly doubles the number of page loads, so it will be slower and use more proxy bandwidth.",
      "default": false
    },

    "save_unique_only": {
      "title": "✅ Save Only Unique Job Listings",
      "type": "boolean",
      "description": "When enabled, if the same job title at the same company appears more than once (e.g. across different search URLs), only the first occurrence is saved. Recommended to keep on.",
      "default": true
    },

    "follow_apply_redirect": {
      "title": "🔗 Follow Redirects for Apply Link",
      "type": "boolean",
      "description": "When enabled, the scraper resolves the external 'Apply on company site' link to its final destination URL (following all redirects). Useful when you want the real company careers page URL instead of Indeed's redirect.\n\nSlightly slower — only enable if you need resolved URLs.",
      "default": false
    },

    "ignore_companies": {
      "title": "🚫 Companies to Skip (optional)",
      "type": "string",
      "description": "Jobs from these companies will be ignored. One name per line.\n\nExample:\nGoogle\nAmazon\nStaffing Solutions Inc",
      "editor": "textarea",
      "prefill": ""
    },

    "ignore_related": {
      "title": "🚫 Keywords to Skip (optional)",
      "type": "string",
      "description": "If a job description contains any of these words, that job is skipped. One keyword per line.\n\nExample:\nclearance required\nC++\nmanagement consulting",
      "editor": "textarea",
      "prefill": ""
    },

    "processed_job_urls": {
      "title": "⏭️ Already-Processed Job URLs (skip duplicates across runs)",
      "type": "array",
      "description": "Paste Indeed job URLs you already scraped in a previous run. The scraper extracts job IDs from these and skips them — so you never collect the same job twice across multiple runs.\n\nPaste job URLs in the format:\nhttps://www.indeed.com/viewjob?jk=abc123def456",
      "editor": "stringList",
      "prefill": []
    },

    "account_cookies": {
      "title": "🍪 Indeed Account Cookies (optional — recommended)",
      "type": "array",
      "description": "Paste your Indeed session cookies as a JSON array. Lets the scraper act as a logged-in user for better results.\n\nHow to export cookies:\n1. Log into Indeed in Chrome.\n2. Install 'https://chromewebstore.google.com/detail/nmckokihipjgplolmcmjakknndddifde?utm_source=item-share-cb' extension.\n3. Export cookies as JSON and paste here.",
      "editor": "json",
      "prefill": []
    },

    "proxy_list": {
      "title": "🌐 Proxy List (optional)",
      "type": "string",
      "description": "Your own proxies, one per line. Rotated each time a browser context is recreated (every 1000 requests).\n\nSupported formats:\n  http://user:pass@host:port\n  host:port:user:pass\n  host:port",
      "editor": "textarea",
      "prefill": ""
    },

    "google_sheet_url": {
      "title": "📊 Google Sheet URL (optional)",
      "type": "string",
      "description": "Export results to Google Sheets — no credentials needed.\n\nSetup (one-time):\n1. Open your Google Sheet.\n2. Share → General access → 'Anyone with the link' → Editor.\n3. Paste the URL here.",
      "editor": "textfield"
    },

    "sheet_name": {
      "title": "📋 Google Sheet Tab Name",
      "type": "string",
      "description": "The worksheet tab where results are written. Created automatically if it doesn't exist.",
      "default": "Indeed Jobs",
      "editor": "textfield"
    },

    "concurrency": {
      "title": "⚡ Concurrency (advanced)",
      "type": "integer",
      "description": "Number of parallel browser workers per phase. Higher = faster but more memory and proxy usage. Max: 10.",
      "default": 5,
      "minimum": 1,
      "maximum": 10
    }

  },
  "required": []
}